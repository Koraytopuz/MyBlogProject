@model List<CommentDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    int count = 0;  
}
<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Ana sayfa Kullanıcı Listesi Alanı </h4>

                        <div class="table-responsive">
                            <table class="table mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Kullanıcı Adı </th>
                                        <th>Yorum</th>
                                        <th>Mail</th>
                                        <th>Tarih</th>
                                        <th>Sil</th>
                                        <th>Güncelle</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <td>@count</td>
                                            <td>@item.Name</td>
                                            <td>@item.Text</td>
                                            <td>@item.Mail</td>
                                            <td>@(((DateTime)item.CreatedAt).ToString("dd-MMM-yyyy"))</td>
                                            <td><a href="/Admin/AdminToDoList/DeleteComment/@item.Id" class="btn btn-outline-danger">Sil </a></td>
                                            <td><a href="/Admin/AdminToDoList/UpdateComment/@item.Id" class="btn btn-outline-success">Güncelle </a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <a href="/Admin/AdminToDoList/CreateComment/" class="btn btn-outline-primary">Yeni Kullanıcı Girişi </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



